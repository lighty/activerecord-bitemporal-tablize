# frozen_string_literal: true

RSpec.describe Activerecord::Bitemporal::Tablize do
  it 'has a version number' do
    expect(Activerecord::Bitemporal::Tablize::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
